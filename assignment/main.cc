#include <stdio.h>

double getPositiveAverage(double[]);
double countRangeValues(double[], int, double);
double getMaxAbsolute(double[], int);
int countInverses(int[], int);
double getMaxCount(double[], int);

int main()
{
}